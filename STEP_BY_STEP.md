# 🚀 Полная инструкция для новичков: Запуск на Ubuntu 24.04

**Для тех, кто первый раз разворачивает проект на сервере.**

---

## 📝 Что нужно перед началом

1. У вас есть сервер с Ubuntu 24.04
2. У вас есть доступ к серверу (логин + пароль или SSH ключ)
3. У вас есть IP адрес сервера (например: `123.45.67.89`)

---

## 🔌 Шаг 1: Подключение к серверу

### Если у вас Windows (через PowerShell или CMD):

```bash
ssh root@123.45.67.89
```

Или если у вас не root, а другой пользователь:

```bash
ssh username@123.45.67.89
```

Введите пароль когда попросят.

### Если у вас Mac или Linux:

Откройте терминал и введите то же самое:

```bash
ssh root@123.45.67.89
```

**✅ Вы должны увидеть что-то типа:**
```
Welcome to Ubuntu 24.04 LTS
root@server:~#
```

---

## 🔄 Шаг 2: Обновление системы

**Что делаем:** Обновляем все пакеты в системе до последних версий.

**Скопируйте и вставьте эту команду:**

```bash
sudo apt update && sudo apt upgrade -y
```

**Что происходит:**
- `apt update` - обновляет список доступных пакетов
- `apt upgrade -y` - устанавливает все обновления (флаг `-y` означает "да на все вопросы")

**⏱️ Ждите 1-3 минуты.** Увидите много текста - это нормально.

**✅ Команда завершена когда снова появится приглашение:**
```
root@server:~#
```

---

## 🐳 Шаг 3: Установка Docker

**Что делаем:** Docker - это программа для запуска контейнеров. Нам нужна она чтобы запустить наш проект.

### 3.1. Скачиваем установщик Docker

**Скопируйте эту команду:**

```bash
curl -fsSL https://get.docker.com -o get-docker.sh
```

**✅ Должно появиться:**
```
root@server:~#
```

Файл `get-docker.sh` скачан.

### 3.2. Запускаем установку

**Скопируйте эту команду:**

```bash
sudo sh get-docker.sh
```

**⏱️ Ждите 30-60 секунд.** Увидите много текста про установку Docker.

**✅ В конце должно быть что-то вроде:**
```
Docker version 25.0.0, build xxxxx
```

### 3.3. Добавляем текущего пользователя в группу docker

**Что делаем:** Чтобы не писать `sudo` перед каждой командой docker.

**Скопируйте эту команду:**

```bash
sudo usermod -aG docker $USER
```

### 3.4. Применяем изменения

**Скопируйте эту команду:**

```bash
newgrp docker
```

Или можно просто выйти и зайти заново:

```bash
exit
ssh root@123.45.67.89
```

### 3.5. Проверяем что Docker работает

**Скопируйте эту команду:**

```bash
docker --version
```

**✅ Должно показать:**
```
Docker version 25.0.0, build xxxxx
```

**Скопируйте эту команду:**

```bash
docker compose version
```

**✅ Должно показать:**
```
Docker Compose version v2.24.0
```

---

## 📦 Шаг 4: Установка Git (если его нет)

**Что делаем:** Git нужен чтобы скачать код проекта с GitHub.

### 4.1. Проверяем есть ли уже Git

**Скопируйте эту команду:**

```bash
git --version
```

**Если показывает версию** (например: `git version 2.43.0`) - **переходите к Шагу 5**.

**Если пишет "command not found"** - устанавливаем Git:

```bash
sudo apt install git -y
```

**⏱️ Ждите 10-20 секунд.**

**Проверяем снова:**

```bash
git --version
```

**✅ Должна показаться версия Git.**

---

## 📥 Шаг 5: Клонирование проекта

**Что делаем:** Скачиваем весь код проекта с GitHub на наш сервер.

### 5.1. Переходим в домашнюю директорию

**Скопируйте эту команду:**

```bash
cd ~
```

**Проверяем где мы:**

```bash
pwd
```

**✅ Должно показать что-то типа:**
```
/root
или
/home/username
```

### 5.2. Клонируем проект

**Скопируйте эту команду:**

```bash
git clone https://github.com/rozmuar/search.git
```

**⏱️ Ждите 5-10 секунд.**

**✅ Должно показать:**
```
Cloning into 'search'...
remote: Enumerating objects: ...
...
done.
```

### 5.3. Переходим в папку проекта

**Скопируйте эту команду:**

```bash
cd search
```

### 5.4. Проверяем что файлы на месте

**Скопируйте эту команду:**

```bash
ls -la
```

**✅ Должны увидеть файлы:**
```
docker-compose.yml
Dockerfile
README.md
requirements-basic.txt
.env.example
и другие...
```

---

## ⚙️ Шаг 6: Настройка конфигурации

**Что делаем:** Создаем файл с настройками для нашего проекта.

### 6.1. Копируем пример конфигурации

**Скопируйте эту команду:**

```bash
cp .env.example .env
```

**✅ Команда выполнена молча, просто появится приглашение снова.**

### 6.2. Просматриваем файл (опционально)

**Если хотите посмотреть что внутри:**

```bash
cat .env
```

**Увидите что-то типа:**
```
REDIS_HOST=redis
REDIS_PORT=6379
POSTGRES_USER=search_user
POSTGRES_PASSWORD=change_me_in_production
...
```

### 6.3. Меняем пароль (ВАЖНО для продакшена!)

**Если это продакшен-сервер, обязательно смените пароль:**

```bash
nano .env
```

**Что делать в редакторе:**

1. Найдите строку `POSTGRES_PASSWORD=change_me_in_production`
2. Стрелками на клавиатуре переместитесь к этой строке
3. Удалите `change_me_in_production`
4. Напишите свой надежный пароль (например: `MyStr0ngP@ssw0rd123!`)
5. Нажмите `Ctrl + X` (выход)
6. Нажмите `Y` (подтвердить сохранение)
7. Нажмите `Enter` (подтвердить имя файла)

**✅ Вернулись в командную строку.**

---

## 🚀 Шаг 7: ЗАПУСК ПРОЕКТА!

**Что делаем:** Запускаем все сервисы (Redis, PostgreSQL, API).

### 7.1. Убеждаемся что мы в правильной папке

**Скопируйте эту команду:**

```bash
pwd
```

**✅ Должно показать:**
```
/root/search
или
/home/username/search
```

### 7.2. Запускаем Docker Compose

**Скопируйте эту команду:**

```bash
docker compose up -d
```

**⏱️ Ждите 1-2 минуты.** При первом запуске Docker будет скачивать образы (Redis, PostgreSQL, Python).

**Вы увидите:**
```
[+] Pulling redis (redis:7-alpine)
[+] Pulling postgres (postgres:15-alpine)
[+] Building api
...
[+] Running 3/3
✔ Container search-redis      Started
✔ Container search-postgres   Started  
✔ Container search-api        Started
```

**✅ Все 3 контейнера запущены!**

---

## ✅ Шаг 8: Проверка что все работает

### 8.1. Проверяем статус контейнеров

**Скопируйте эту команду:**

```bash
docker compose ps
```

**✅ Должны увидеть 3 контейнера со статусом "Up":**
```
NAME                STATUS              PORTS
search-redis        Up 1 minute         6379/tcp
search-postgres     Up 1 minute         5432/tcp
search-api          Up 1 minute         0.0.0.0:8000->8000/tcp
```

**Если какой-то контейнер "Exited" или "Unhealthy" - смотрите раздел "Решение проблем" внизу.**

### 8.2. Проверяем здоровье API

**Скопируйте эту команду:**

```bash
curl http://localhost:8000/health
```

**✅ Должно вернуть:**
```json
{"status":"ok","timestamp":"2026-02-01T12:00:00"}
```

**Если пишет "curl: command not found":**

```bash
sudo apt install curl -y
```

Затем повторите команду с curl.

### 8.3. Проверяем из браузера (с вашего компьютера)

**Откройте браузер и введите:**

```
http://123.45.67.89:8000/docs
```

**Замените `123.45.67.89` на IP вашего сервера!**

**✅ Должна открыться страница Swagger с документацией API.**

**Если не открывается - смотрите раздел "Настройка файрвола" ниже.**

---

## 🧪 Шаг 9: Тестирование поиска

### 9.1. Индексируем тестовые данные

**Что делаем:** Загружаем в систему 10 тестовых товаров для проверки.

**Скопируйте эту команду:**

```bash
docker compose exec api python /app/scripts/test_search.py
```

**⏱️ Подождите 2-5 секунд.**

**✅ Должно показать:**
```
Indexed 10 products
Testing search...
Found X results for 'iphone'
```

### 9.2. Делаем тестовый поиск

**Скопируйте эту команду:**

```bash
curl "http://localhost:8000/api/v1/search?q=iphone&limit=3"
```

**✅ Должен вернуться JSON с результатами:**
```json
{
  "items": [
    {
      "id": "1",
      "name": "iPhone 15 Pro Max",
      "price": 99990,
      "score": 10.5
    }
  ],
  "total": 1,
  "meta": {
    "took_ms": 15
  }
}
```

---

## 🔥 Шаг 10: Настройка файрвола (безопасность)

**Что делаем:** Открываем только нужные порты и закрываем все остальные.

### 10.1. Устанавливаем UFW (если его нет)

**Скопируйте эту команду:**

```bash
sudo apt install ufw -y
```

### 10.2. Разрешаем SSH (ВАЖНО! Иначе потеряете доступ к серверу)

**Скопируйте эту команду:**

```bash
sudo ufw allow OpenSSH
```

### 10.3. Разрешаем HTTP (порт 80)

**Скопируйте эту команду:**

```bash
sudo ufw allow 80/tcp
```

### 10.4. Разрешаем HTTPS (порт 443)

**Скопируйте эту команду:**

```bash
sudo ufw allow 443/tcp
```

### 10.5. Разрешаем порт API (8000) - временно для тестов

**Скопируйте эту команду:**

```bash
sudo ufw allow 8000/tcp
```

### 10.6. Включаем файрвол

**Скопируйте эту команду:**

```bash
sudo ufw enable
```

**Напишет: "Command may disrupt existing ssh connections. Proceed with operation (y|n)?"**

**Введите:** `y` и нажмите Enter.

### 10.7. Проверяем статус

**Скопируйте эту команду:**

```bash
sudo ufw status
```

**✅ Должно показать:**
```
Status: active

To                         Action      From
--                         ------      ----
OpenSSH                    ALLOW       Anywhere
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
8000/tcp                   ALLOW       Anywhere
```

---

## 🌐 Шаг 11: Проверка из интернета

### 11.1. С вашего компьютера

**Откройте браузер и перейдите:**

```
http://ВАШ_IP_СЕРВЕРА:8000/docs
```

**Пример:**
```
http://123.45.67.89:8000/docs
```

**✅ Должна открыться страница Swagger API.**

### 11.2. Тестируем поиск через браузер

**В адресной строке:**

```
http://ВАШ_IP_СЕРВЕРА:8000/api/v1/search?q=iphone
```

**✅ Должен вернуться JSON с результатами.**

---

## 🎨 Шаг 12: Запуск веб-интерфейса (опционально)

**Если хотите веб-интерфейс с красивым дизайном:**

### 12.1. Установка веб-сервера

**Скопируйте эту команду:**

```bash
sudo apt install nginx -y
```

### 12.2. Копируем веб-файлы

**Скопируйте эти команды по очереди:**

```bash
sudo mkdir -p /var/www/search
```

```bash
sudo cp -r ~/search/src/web/* /var/www/search/
```

```bash
sudo chown -R www-data:www-data /var/www/search
```

### 12.3. Создаем конфигурацию Nginx

**Скопируйте эту команду:**

```bash
sudo nano /etc/nginx/sites-available/search
```

**Скопируйте и вставьте этот текст:**

```nginx
server {
    listen 80;
    server_name _;

    # API
    location /api {
        proxy_pass http://localhost:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Docs
    location /docs {
        proxy_pass http://localhost:8000/docs;
        proxy_set_header Host $host;
    }

    # Web interface
    location / {
        root /var/www/search;
        try_files $uri $uri/ /index.html;
    }
}
```

**Сохраните файл:**
- Нажмите `Ctrl + X`
- Нажмите `Y`
- Нажмите `Enter`

### 12.4. Активируем конфигурацию

**Скопируйте эти команды:**

```bash
sudo ln -s /etc/nginx/sites-available/search /etc/nginx/sites-enabled/
```

```bash
sudo rm /etc/nginx/sites-enabled/default
```

```bash
sudo nginx -t
```

**✅ Должно показать:**
```
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

### 12.5. Перезапускаем Nginx

**Скопируйте эту команду:**

```bash
sudo systemctl restart nginx
```

### 12.6. Проверяем веб-интерфейс

**Откройте браузер:**

```
http://ВАШ_IP_СЕРВЕРА
```

**✅ Должна открыться красивая лендинг-страница с поиском!**

---

## 📊 Шаг 13: Полезные команды

### Просмотр логов

**Все логи:**
```bash
docker compose logs -f
```

**Только API:**
```bash
docker compose logs -f api
```

**Выход из просмотра логов:** Нажмите `Ctrl + C`

### Остановка проекта

```bash
docker compose down
```

### Запуск проекта

```bash
docker compose up -d
```

### Перезапуск проекта

```bash
docker compose restart
```

### Проверка использования ресурсов

```bash
docker stats
```

**Выход:** Нажмите `Ctrl + C`

---

## 🆘 Решение проблем

### ❌ Проблема: "Cannot connect to Docker daemon"

**Решение:**

```bash
sudo systemctl start docker
sudo systemctl enable docker
```

### ❌ Проблема: Контейнер API в статусе "Exited"

**Смотрим логи:**

```bash
docker compose logs api
```

**Обычно помогает перезапуск:**

```bash
docker compose down
docker compose up -d
```

### ❌ Проблема: "Port 8000 already in use"

**Смотрим что занимает порт:**

```bash
sudo lsof -i :8000
```

**Убиваем процесс:**

```bash
sudo kill -9 <PID>
```

Замените `<PID>` на номер процесса из предыдущей команды.

### ❌ Проблема: Не могу подключиться к серверу из браузера

**Проверяем файрвол:**

```bash
sudo ufw status
```

**Если порт 8000 не открыт:**

```bash
sudo ufw allow 8000/tcp
```

**Проверяем что API запущен:**

```bash
docker compose ps
curl http://localhost:8000/health
```

### ❌ Проблема: "curl: command not found"

**Установите curl:**

```bash
sudo apt install curl -y
```

### ❌ Проблема: Мало места на диске

**Проверка места:**

```bash
df -h
```

**Очистка Docker:**

```bash
docker system prune -a -f
```

---

## 🔄 Обновление проекта

**Когда выйдет новая версия:**

```bash
cd ~/search
git pull
docker compose down
docker compose up -d --build
```

---

## ✅ Финальный чеклист

Отметьте что выполнили:

- [ ] Подключился к серверу по SSH
- [ ] Обновил систему (`apt update && apt upgrade`)
- [ ] Установил Docker
- [ ] Проверил что Docker работает (`docker --version`)
- [ ] Установил Git (если нужно было)
- [ ] Склонировал проект (`git clone`)
- [ ] Создал файл `.env`
- [ ] Запустил проект (`docker compose up -d`)
- [ ] Проверил статус контейнеров (`docker compose ps`)
- [ ] API отвечает на `/health`
- [ ] Загрузил тестовые данные
- [ ] Поиск работает (через curl или браузер)
- [ ] Настроил файрвол
- [ ] Могу открыть API из интернета
- [ ] (Опционально) Установил Nginx
- [ ] (Опционально) Веб-интерфейс работает

---

## 🎉 Поздравляю!

Ваш сервис поиска запущен и работает!

**Что дальше:**

1. **API документация:** `http://ВАШ_IP:8000/docs`
2. **Веб-интерфейс:** `http://ВАШ_IP` (если установили Nginx)
3. **Dashboard:** `http://ВАШ_IP/dashboard.html`

**Важные ссылки:**

- Полная документация: `README.md`
- Быстрый старт: `QUICKSTART.md`
- API интеграция: `docs/api.md`
- Шпаргалка: `CHEATSHEET.md`

---

## 💬 Нужна помощь?

Если что-то не работает:

1. Проверьте логи: `docker compose logs`
2. Перезапустите: `docker compose restart`
3. Посмотрите раздел "Решение проблем" выше

**Успехов! 🚀**
