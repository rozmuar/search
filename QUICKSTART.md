# 🚀 Search Service - Quick Start (Basic Version)

Базовая версия поискового сервиса **БЕЗ ML компонентов**.  
Классический BM25-подобный поиск с инвертированным индексом.

## ✨ Возможности

- ⚡ **Быстрый поиск** по инвертированному индексу
- 🔤 **N-gram поиск** для частичного совпадения
- 💡 **Автодополнение** (suggestions)
- 🎯 **Фильтры** (цена, наличие, категория)
- 🏷️ **Токенизация** с удалением стоп-слов
- 📦 **REST API** на FastAPI
- 🐳 **Docker** для быстрого запуска

## 📋 Требования

- Docker и Docker Compose
- Python 3.11+ (если запускаете локально)

## 🎯 Быстрый старт

### 1. Клонируйте репозиторий

```bash
git clone <repo-url>
cd search
```

### 2. Запустите сервисы

```bash
docker-compose up -d
```

Это запустит:
- **Redis** (порт 6379) - для индексов
- **PostgreSQL** (порт 5432) - для метаданных
- **API** (порт 8000) - поисковый API

### 3. Проверьте работу

```bash
# Проверка health check
curl http://localhost:8000/health

# Документация API
http://localhost:8000/docs
```

### 4. Загрузите тестовые данные

```bash
# Установите зависимости для скрипта
pip install httpx

# Запустите тест
python scripts/test_search.py
```

## 📖 Использование API

### Индексация товаров

```bash
curl -X POST "http://localhost:8000/api/v1/index?project_id=demo" \
  -H "Content-Type: application/json" \
  -d '[
    {
      "id": "prod-1",
      "name": "Apple iPhone 15 Pro",
      "description": "Флагманский смартфон",
      "url": "https://shop.com/iphone",
      "image": "https://shop.com/img.jpg",
      "price": 119990,
      "old_price": 129990,
      "in_stock": true,
      "category": "Смартфоны",
      "brand": "Apple"
    }
  ]'
```

### Поиск

```bash
# Простой поиск
curl "http://localhost:8000/api/v1/search?q=iphone&project_id=demo"

# С фильтрами
curl "http://localhost:8000/api/v1/search?q=смартфон&project_id=demo&in_stock=true&price_max=100000"

# С пагинацией
curl "http://localhost:8000/api/v1/search?q=apple&limit=10&offset=0"
```

### Подсказки (автодополнение)

```bash
curl "http://localhost:8000/api/v1/suggest?q=app&project_id=demo"
```

## 🧪 Примеры запросов

### Python

```python
import httpx

async def search_products():
    async with httpx.AsyncClient() as client:
        response = await client.get(
            "http://localhost:8000/api/v1/search",
            params={
                "q": "наушники",
                "project_id": "demo",
                "limit": 10
            }
        )
        result = response.json()
        
        for item in result['items']:
            print(f"{item['name']} - {item['price']} руб.")
```

### JavaScript

```javascript
fetch('http://localhost:8000/api/v1/search?q=iphone&project_id=demo')
  .then(res => res.json())
  .then(data => {
    console.log(`Найдено: ${data.total}`);
    data.items.forEach(item => {
      console.log(`${item.name} - ${item.price} руб.`);
    });
  });
```

## 📊 Структура проекта

```
search/
├── docker-compose.yml      # Docker конфигурация
├── Dockerfile             # Образ API сервиса
├── requirements-basic.txt # Python зависимости (без ML)
├── src/
│   ├── api/              # FastAPI приложение
│   │   └── main.py       # Endpoints
│   ├── search/           # Поисковый движок
│   │   ├── engine_simple.py       # Поиск
│   │   ├── indexer_simple.py      # Индексация
│   │   └── query_processor_simple.py  # Обработка запросов
│   └── core/             # Базовые модели
└── scripts/
    ├── init.sql          # Инициализация БД
    └── test_search.py    # Тестовый скрипт
```

## ⚙️ Конфигурация

### Переменные окружения

Отредактируйте `docker-compose.yml`:

```yaml
environment:
  - REDIS_HOST=redis
  - REDIS_PORT=6379
  - DB_HOST=postgres
  - DB_PORT=5432
  - DB_NAME=search_service
  - DB_USER=search_user
  - DB_PASSWORD=search_pass
```

## 🔧 Локальная разработка

### Без Docker

```bash
# 1. Установите зависимости
pip install -r requirements-basic.txt

# 2. Запустите Redis и PostgreSQL
# (используйте локальные инсталляции или Docker)

# 3. Установите переменные окружения
export REDIS_HOST=localhost
export DB_HOST=localhost

# 4. Запустите API
uvicorn src.api.main:app --reload --host 0.0.0.0 --port 8000
```

## 📈 Производительность

**Текущая версия (без ML):**
- ⚡ Индексация: ~500-1000 товаров/сек
- 🔍 Поиск: ~10-30ms (p95)
- 💾 RAM: ~200MB (без учета Redis)
- 🖥️ CPU: 2-4 ядра достаточно

**Ограничения:**
- ❌ Нет семантического поиска
- ❌ Нет исправления опечаток (spell check)
- ❌ Нет neural re-ranking
- ✅ Базовый BM25-подобный поиск
- ✅ N-gram для частичного совпадения

## 🚀 Что дальше?

### Добавить ML компоненты

1. Установите ML зависимости:
```bash
pip install -r requirements.txt  # Полная версия
```

2. Включите гибридный поиск в коде
3. Настройте vector store (Qdrant/Redis)
4. Для production потребуется GPU

### Масштабирование

- Добавьте Redis Cluster для больших каталогов
- Настройте кэширование популярных запросов
- Используйте load balancer для API

## 🐛 Troubleshooting

### API не запускается

```bash
# Проверьте логи
docker-compose logs api

# Перезапустите сервисы
docker-compose restart
```

### Redis недоступен

```bash
# Проверьте статус
docker-compose ps

# Подключитесь к Redis
docker-compose exec redis redis-cli ping
```

### Поиск не находит товары

```bash
# Проверьте индекс
docker-compose exec redis redis-cli
> KEYS *:demo:*
```

## 📚 Документация

- **API Docs**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **Health Check**: http://localhost:8000/health

## 🤝 Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что порты свободны: 8000, 6379, 5432
3. Проверьте версию Docker: `docker --version`

---

**Готово!** 🎉 Базовая версия работает без GPU и ML библиотек.

Для добавления ML функций см. полную документацию в `docs/ml-integration.md`.
