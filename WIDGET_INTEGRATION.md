# 🔧 Инструкция по встраиванию виджета SearchPro

## Быстрый старт

### 1. Добавьте контейнер

Вставьте этот HTML-код в то место на странице, где должен появиться поиск:

```html
<div id="searchpro-widget"></div>
```

### 2. Подключите скрипт

Добавьте перед закрывающим тегом `</body>`:

```html
<script src="https://ваш-домен.com/api/v1/widget/embed.js" data-api-key="ВАШ_API_КЛЮЧ"></script>
```

**Готово!** Виджет автоматически инициализируется.

---

## Расширенная настройка

### Параметры через JavaScript

```html
<script>
window.SearchProConfig = {
    // Обязательно
    apiKey: 'ВАШ_API_КЛЮЧ',
    
    // Контейнер (по умолчанию '#searchpro-widget')
    container: '#my-search',
    
    // Внешний вид
    theme: 'light',              // 'light', 'dark', 'auto'
    primaryColor: '#2563eb',     // Основной цвет
    textColor: '#1f2937',        // Цвет текста
    bgColor: '#ffffff',          // Цвет фона
    borderRadius: 8,             // Скругление углов (px)
    
    // Поведение
    placeholder: 'Поиск товаров...',
    resultsPerPage: 10,
    minQueryLength: 2,           // Мин. длина запроса
    debounceMs: 300,             // Задержка перед поиском (мс)
    showImages: true,            // Показывать изображения
    showPrices: true,            // Показывать цены
    showSuggestions: true,       // Автодополнение
    
    // Callbacks
    onSearch: function(query, results) {
        console.log('Поиск:', query);
        console.log('Результаты:', results);
    },
    
    onSelect: function(product) {
        // Переход на страницу товара
        window.location.href = product.url;
    },
    
    onError: function(error) {
        console.error('Ошибка поиска:', error);
    }
};
</script>
<script src="https://ваш-домен.com/api/v1/widget/embed.js"></script>
```

---

## Примеры интеграции

### WordPress

Добавьте в файл `footer.php` вашей темы или через плагин для вставки кода:

```php
<!-- SearchPro Widget -->
<div id="searchpro-widget"></div>
<script src="https://ваш-домен.com/api/v1/widget/embed.js" data-api-key="ВАШ_API_КЛЮЧ"></script>
```

### 1С-Bitrix

В файле шаблона или через визуальный редактор:

```html
<div id="searchpro-widget"></div>
<script src="https://ваш-домен.com/api/v1/widget/embed.js" data-api-key="ВАШ_API_КЛЮЧ"></script>
```

### OpenCart

В файле `footer.tpl` или через модуль HTML:

```html
<div id="searchpro-widget"></div>
<script src="https://ваш-домен.com/api/v1/widget/embed.js" data-api-key="ВАШ_API_КЛЮЧ"></script>
```

### React

```jsx
import { useEffect } from 'react';

function SearchWidget() {
    useEffect(() => {
        window.SearchProConfig = {
            apiKey: 'ВАШ_API_КЛЮЧ',
            container: '#searchpro-widget',
            onSelect: (product) => {
                // React Router navigation
                navigate(`/product/${product.id}`);
            }
        };
        
        const script = document.createElement('script');
        script.src = 'https://ваш-домен.com/api/v1/widget/embed.js';
        document.body.appendChild(script);
        
        return () => {
            document.body.removeChild(script);
        };
    }, []);
    
    return <div id="searchpro-widget"></div>;
}
```

### Vue.js

```vue
<template>
  <div id="searchpro-widget"></div>
</template>

<script>
export default {
  mounted() {
    window.SearchProConfig = {
      apiKey: 'ВАШ_API_КЛЮЧ',
      container: '#searchpro-widget',
      onSelect: (product) => {
        this.$router.push(`/product/${product.id}`);
      }
    };
    
    const script = document.createElement('script');
    script.src = 'https://ваш-домен.com/api/v1/widget/embed.js';
    document.body.appendChild(script);
  }
}
</script>
```

---

## API для программного управления

После загрузки виджета доступен глобальный объект `SearchPro`:

```javascript
// Программный поиск
SearchPro.search('iphone');

// Получить текущие настройки
console.log(SearchPro.config);

// Обновить конфигурацию с сервера
SearchPro.refresh();
```

---

## Кастомизация стилей

### CSS переменные

Виджет использует CSS переменные, которые можно переопределить:

```css
.sp-widget {
    --sp-primary-color: #2563eb;
    --sp-text-color: #1f2937;
    --sp-bg-color: #ffffff;
    --sp-border-radius: 8px;
}
```

### Полная кастомизация

Вы можете переопределить любые стили виджета:

```css
/* Поле ввода */
.sp-search-input {
    font-size: 18px !important;
    border-width: 2px !important;
}

/* Результаты поиска */
.sp-result-item {
    padding: 20px !important;
}

/* Цена */
.sp-result-price {
    font-size: 24px !important;
}

/* Тёмная тема */
.sp-widget.sp-dark .sp-search-input {
    background: #111 !important;
}
```

---

## Адаптивность

Виджет адаптивен из коробки:

- На мобильных устройствах (`<480px`) результаты отображаются в одну колонку
- Изображения товаров масштабируются автоматически
- Поле ввода занимает всю ширину контейнера

### Контроль ширины

```html
<div id="searchpro-widget" style="max-width: 600px; margin: 0 auto;"></div>
```

---

## FAQ

### Виджет не загружается

1. Проверьте правильность API-ключа
2. Убедитесь, что домен добавлен в разрешённые в настройках проекта
3. Откройте консоль браузера (F12) и проверьте ошибки

### Товары не находятся

1. Убедитесь, что фид загружен в личном кабинете
2. Проверьте, что фид содержит товары
3. Попробуйте поискать по точному названию товара

### Как изменить внешний вид?

1. Используйте настройки в личном кабинете (раздел "Виджет")
2. Или переопределите стили через CSS

### Конфликт с другими скриптами

Виджет работает в изолированном пространстве имён. Если есть конфликты:

```html
<script>
window.SearchProConfig = {
    apiKey: 'ВАШ_API_КЛЮЧ',
    container: '#my-custom-search'  // Используйте уникальный ID
};
</script>
```

---

## Поддержка

При возникновении проблем:
1. Проверьте консоль браузера на ошибки
2. Убедитесь в актуальности API-ключа
3. Свяжитесь с поддержкой через личный кабинет
